# Badger's complete tool allowlist. One exact tool name per line.
# Anything not listed here is blocked — including tools that do not exist yet.
#
# This is an allowlist, not a blocklist, on purpose. Matching on verbs like
# "send" or "delete" fails open on neutrally-named mutators: gmail__modify_labels,
# drive__update_permissions, or a future github__merge_pull_request would all
# sail through a verb filter while changing state. Listing exact names fails
# closed instead: a tool we have not vetted cannot run.
#
# MCP tools are namespaced <server>__<tool>, where <server> is the key in
# agent.yaml's mcp_servers map. Renaming a server there silently invalidates
# every entry below, so the two must be changed together.

# --- builtins -------------------------------------------------------------
# The GAP learning loop is ON: task_tracker records what a run did,
# skill_learner turns a successful approach into a committed skill, and memory
# holds pointers. These write to the AGENT'S OWN repo — never to a source —
# which is the line this file actually defends: Badger may change itself; it
# may not change your GitHub, Gmail or Drive. cli (a shell), write and edit
# stay absent: a shell is not self-modification, it is everything.
read
memory
task_tracker
skill_learner

# ===========================================================================
# BADGER'S OWN TOOLS — declarative, tools/*.yaml
# ===========================================================================
# These are the names the agent actually sees. Each is a thin script over a
# Composio session (tools/scripts/), so the Composio slugs listed below never
# reach the model. dist/index.js wraps declarative tools with pre_tool_use
# exactly like MCP tools, so this file gates them.
#
# All four are read-only by construction: the script can only call the five
# allowlisted Composio slugs in tools/scripts/_github.mjs.

github_search
github_issue
github_pr
github_file
github_commits

# --- gmail and google drive -----------------------------------------------
# VERIFIED 2026-08-17 against a live Composio session. These five are the
# agent's whole Google surface; each is a thin script over the eight read
# slugs listed in tools/scripts/_google.mjs.
#
# The names that USED to sit here were provisional MCP names invented before
# the source existed — gmail__search_threads, drive__read_file_content and
# four others, none of which was ever real. They are removed rather than left
# as documentation: an allowlist naming tools that do not exist teaches the
# reader that entries here are aspirational, which is precisely the habit that
# makes an allowlist stop being trustworthy.

gmail_search
gmail_thread
drive_search
drive_file
drive_comments

# --- github ---------------------------------------------------------------
# VERIFIED against github-mcp-server 1.9.0 via:
#   node scripts/mcp-tools.mjs --stdio github-mcp-server stdio --read-only
# with GITHUB_TOOLSETS=context,repos,issues,pull_requests,users (26 tools
# offered). Listed below are the ones Badger's skills need; the rest of the 26
# are read-only too but stay unlisted until a skill actually calls for them.

# search — the entry point for most questions
github__search_code
github__search_repositories
github__search_issues
github__search_pull_requests
github__search_commits
github__search_users

# read — for pulling detail on something search surfaced
github__get_file_contents
github__issue_read
github__pull_request_read
github__get_commit
github__list_commits
github__list_issues
github__list_pull_requests

# ownership — answers "who owns X"
# May require Administration:read on a fine-grained token. If it 403s, drop it:
# CODEOWNERS via get_file_contents and commit history cover the same question.
github__list_repository_collaborators

# identity — resolves "my" / "our" in a question to the authenticated user
github__get_me

# ===========================================================================
# COMPOSIO — supersedes the github__* block above once the agent is wired.
# ===========================================================================
# VERIFIED 2026-08-17 against a live Composio session:
#   node scripts/composio-session.mjs status
#
# Composio's GitHub toolkit exposes 823 tools — the whole GitHub REST API,
# including GITHUB_BLOCK_A_USER and GITHUB_ADD_A_REPOSITORY_COLLABORATOR.
# The 12 below are what a session with the DIRECT_TOOLS preset registers.
#
# Two things this file's allow-by-name rule now has hard evidence for:
#   1. GITHUB_LIST_REPOSITORY_SECRETS and GITHUB_GET_A_REPOSITORY_SECRET are
#      read-shaped tools that read credentials. A "LIST/GET is safe" heuristic
#      would hand Badger repo secrets. Naming each tool is not pedantry.
#   2. A default (non-DIRECT_TOOLS) session registers COMPOSIO_MULTI_EXECUTE_TOOL
#      and COMPOSIO_REMOTE_BASH_TOOL — one generic name that runs anything.
#      Name-based gating cannot see through that, so this file would pass every
#      write while looking green. The preset is load-bearing, not a tidy-up.
#
# Namespacing is NOT settled: these are Composio's own slugs. How they appear
# to gitagent depends on the wiring (session MCP endpoint vs SDK), which is
# still open. Re-run the probe after wiring and fix the prefixes here.

GITHUB_SEARCH_ISSUES_AND_PULL_REQUESTS
GITHUB_LIST_REPOSITORY_ISSUES
GITHUB_GET_AN_ISSUE
GITHUB_LIST_ISSUE_COMMENTS
GITHUB_LIST_PULL_REQUESTS
GITHUB_GET_A_PULL_REQUEST
GITHUB_LIST_REVIEW_COMMENTS_ON_A_PULL_REQUEST
GITHUB_GET_REPOSITORY_CONTENT
GITHUB_LIST_COMMITS
GITHUB_GET_A_COMMIT
GITHUB_GET_A_REPOSITORY
GITHUB_SEARCH_CODE

# --- composio: gmail and google drive -------------------------------------
# VERIFIED 2026-08-17 against a live session. Gmail exposes 63 tools and Drive
# 90; these eight are the whole read surface, enabled in
# tools/scripts/_google.mjs. They are Composio slugs, not agent tool names —
# the agent never sees them, and they are listed for auditing.
#
# This pair of namespaces is the strongest argument in this file for
# allow-by-name. A "does the name sound like a write?" filter run over Drive's
# 90 tools classified GOOGLEDRIVE_EDIT_FILE as read-only, along with
# HIDE_DRIVE, WATCH_CHANGES and STOP_WATCH_CHANNEL. Gmail keeps SEND_EMAIL,
# TRASH_MESSAGE and DELETE_DRAFT in the same namespace as FETCH_EMAILS.

GMAIL_FETCH_EMAILS
GMAIL_FETCH_MESSAGE_BY_THREAD_ID
GMAIL_FETCH_MESSAGE_BY_MESSAGE_ID
GOOGLEDRIVE_FIND_FILE
GOOGLEDRIVE_GET_FILE_METADATA
GOOGLEDRIVE_EXPORT_GOOGLE_WORKSPACE_FILE
GOOGLEDRIVE_LIST_COMMENTS
GOOGLEDRIVE_LIST_REPLIES
